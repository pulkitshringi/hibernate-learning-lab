<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "https://hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property> <!-- for loading the mySQL Driver -->
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/hibernate_practice</property> <!-- connecting to our database -->
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password"></property>

        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property> <!-- tells hibernate that our database is MySQL -->
        <property name="hibernate.show_sql">true</property> <!-- this will show hibernate generated sql in terminal -->
        <property name="hibernate.format_sql">true</property> <!-- shown sql will be now formatted in terminal -->
        <property name="hibernate.hbm2ddl.auto">update</property>  <!-- just update the table if exists or if not exists then create the table -->
    </session-factory>
</hibernate-configuration>
